mixin video(video)
  div
    h4
      a(href=`/videos/${video.id}`, target="_blank")=video.title
    ul
      li #{video.rating}/5.
      li #{video.comments} comments.
      li Posted #{video.createdAt}.
      li #{video .vieos} views